import { BuildConfig } from './BuildConfig';
import { IBuildConfig, IUserTaskDefinition, ITargetPickItem } from './interface';

export class NinjaGenerator
{
  constructor() {
  }

  update(config: BuildConfig): void {
  }

}
